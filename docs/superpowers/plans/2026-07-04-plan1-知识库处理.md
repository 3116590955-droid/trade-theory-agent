# 新贸易理论智能体 · 计划一：知识库处理

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将本地 PDF/PPT 课程材料批量转换为结构化 Markdown 文本，供网页应用检索使用。

**Architecture:** 使用 Python 脚本依次提取每份文件的文本内容，清洗后写入 `knowledge_base/` 目录中对应的 `.md` 文件，每份文件一个 Markdown，文件名与原文件对应。

**Tech Stack:** Python 3.10+、`pymupdf`（PDF 提取）、`python-pptx`（PPT 提取）、`pathlib`

## Global Constraints

- Python >= 3.10
- 输出文件编码：UTF-8
- 输出目录：`knowledge_base/`，路径相对于项目根目录 `国贸智能体/`
- 每个 Markdown 文件顶部必须包含元数据头：文件名、类型、原始路径
- 不修改或删除 `国际经济学课上知识/` 中的原始文件

---

### Task 1: 初始化项目环境

**Files:**
- Create: `knowledge_base/.gitkeep`
- Create: `scripts/requirements.txt`
- Create: `scripts/extract.py`（骨架，Task 2 填充）

**Interfaces:**
- Produces: `scripts/requirements.txt` 供后续任务安装依赖

- [ ] **Step 1: 创建目录结构**

在项目根目录 `国贸智能体/` 下运行：

```bash
mkdir -p knowledge_base scripts
touch knowledge_base/.gitkeep
```

- [ ] **Step 2: 写入依赖文件**

创建 `scripts/requirements.txt`，内容：

```
pymupdf==1.24.5
python-pptx==0.6.23
```

- [ ] **Step 3: 安装依赖**

```bash
cd 国贸智能体
pip install -r scripts/requirements.txt
```

预期输出：`Successfully installed pymupdf-1.24.5 python-pptx-0.6.23`

- [ ] **Step 4: 创建提取脚本骨架**

创建 `scripts/extract.py`：

```python
from pathlib import Path

SOURCE_DIR = Path(__file__).parent.parent / "国际经济学课上知识"
OUTPUT_DIR = Path(__file__).parent.parent / "knowledge_base"

def main():
    OUTPUT_DIR.mkdir(exist_ok=True)
    print(f"Source: {SOURCE_DIR}")
    print(f"Output: {OUTPUT_DIR}")
    print("Files found:")
    for f in sorted(SOURCE_DIR.iterdir()):
        print(f"  {f.name}")

if __name__ == "__main__":
    main()
```

- [ ] **Step 5: 验证骨架可运行**

```bash
python scripts/extract.py
```

预期输出：列出 `国际经济学课上知识/` 中所有 11 个文件名。

- [ ] **Step 6: Commit**

```bash
git add knowledge_base/.gitkeep scripts/requirements.txt scripts/extract.py
git commit -m "feat: init knowledge base extraction scaffold"
```

---

### Task 2: 实现 PDF 文本提取

**Files:**
- Modify: `scripts/extract.py`

**Interfaces:**
- Produces: `extract_pdf(path: Path) -> str` — 返回 PDF 全文字符串

- [ ] **Step 1: 写失败测试**

创建 `scripts/test_extract.py`：

```python
import pytest
from pathlib import Path
from extract import extract_pdf

SOURCE_DIR = Path(__file__).parent.parent / "国际经济学课上知识"

def test_extract_pdf_returns_nonempty_string():
    pdf_path = SOURCE_DIR / "Krugman(1979) Increasing returns, monopolistic competition, and international trade.pdf"
    result = extract_pdf(pdf_path)
    assert isinstance(result, str)
    assert len(result) > 100
    assert "trade" in result.lower() or "krugman" in result.lower()
```

- [ ] **Step 2: 运行确认失败**

```bash
cd scripts && python -m pytest test_extract.py::test_extract_pdf_returns_nonempty_string -v
```

预期：`FAILED` — `ImportError: cannot import name 'extract_pdf'`

- [ ] **Step 3: 实现 extract_pdf**

在 `scripts/extract.py` 中添加：

```python
import fitz  # pymupdf

def extract_pdf(path: Path) -> str:
    doc = fitz.open(str(path))
    pages = []
    for page in doc:
        pages.append(page.get_text())
    doc.close()
    return "\n".join(pages)
```

- [ ] **Step 4: 运行确认通过**

```bash
python -m pytest test_extract.py::test_extract_pdf_returns_nonempty_string -v
```

预期：`PASSED`

- [ ] **Step 5: Commit**

```bash
git add scripts/extract.py scripts/test_extract.py
git commit -m "feat: implement PDF text extraction with pymupdf"
```

---

### Task 3: 实现 PPT 文本提取

**Files:**
- Modify: `scripts/extract.py`
- Modify: `scripts/test_extract.py`

**Interfaces:**
- Produces: `extract_pptx(path: Path) -> str` — 返回 PPT 全部幻灯片文本

- [ ] **Step 1: 写失败测试**

在 `scripts/test_extract.py` 追加：

```python
from extract import extract_pptx

def test_extract_pptx_returns_nonempty_string():
    pptx_path = SOURCE_DIR / "partII lecture 2 IIT.pptx"
    result = extract_pptx(pptx_path)
    assert isinstance(result, str)
    assert len(result) > 50
```

- [ ] **Step 2: 运行确认失败**

```bash
python -m pytest test_extract.py::test_extract_pptx_returns_nonempty_string -v
```

预期：`FAILED` — `ImportError: cannot import name 'extract_pptx'`

- [ ] **Step 3: 实现 extract_pptx**

在 `scripts/extract.py` 中添加：

```python
from pptx import Presentation

def extract_pptx(path: Path) -> str:
    prs = Presentation(str(path))
    slides = []
    for i, slide in enumerate(prs.slides, 1):
        texts = []
        for shape in slide.shapes:
            if shape.has_text_frame:
                for para in shape.text_frame.paragraphs:
                    line = para.text.strip()
                    if line:
                        texts.append(line)
        if texts:
            slides.append(f"[Slide {i}]\n" + "\n".join(texts))
    return "\n\n".join(slides)
```

- [ ] **Step 4: 运行确认通过**

```bash
python -m pytest test_extract.py -v
```

预期：两个测试均 `PASSED`

- [ ] **Step 5: Commit**

```bash
git add scripts/extract.py scripts/test_extract.py
git commit -m "feat: implement PPTX text extraction with python-pptx"
```

---

### Task 4: 批量转换并写出 Markdown 文件

**Files:**
- Modify: `scripts/extract.py`（完善 `main()` 函数）

**Interfaces:**
- Consumes: `extract_pdf(path: Path) -> str`、`extract_pptx(path: Path) -> str`
- Produces: `knowledge_base/<stem>.md`，每个文件包含元数据头 + 正文

- [ ] **Step 1: 写失败测试**

在 `scripts/test_extract.py` 追加：

```python
import tempfile
from extract import write_markdown

def test_write_markdown_creates_file_with_header():
    with tempfile.TemporaryDirectory() as tmp:
        out_dir = Path(tmp)
        src = SOURCE_DIR / "Krugman(1979) Increasing returns, monopolistic competition, and international trade.pdf"
        write_markdown(src, out_dir)
        out_file = out_dir / (src.stem + ".md")
        assert out_file.exists()
        content = out_file.read_text(encoding="utf-8")
        assert content.startswith("---")
        assert "source:" in content
        assert "type: pdf" in content
        assert len(content) > 200
```

- [ ] **Step 2: 运行确认失败**

```bash
python -m pytest test_extract.py::test_write_markdown_creates_file_with_header -v
```

预期：`FAILED` — `ImportError: cannot import name 'write_markdown'`

- [ ] **Step 3: 实现 write_markdown 和完善 main()**

替换 `scripts/extract.py` 中的 `main()` 和新增 `write_markdown()`：

```python
def write_markdown(src: Path, out_dir: Path) -> None:
    suffix = src.suffix.lower()
    if suffix == ".pdf":
        body = extract_pdf(src)
        file_type = "pdf"
    elif suffix == ".pptx":
        body = extract_pptx(src)
        file_type = "pptx"
    else:
        return  # 跳过不支持的格式

    header = f"---\nsource: {src.name}\ntype: {file_type}\noriginal_path: 国际经济学课上知识/{src.name}\n---\n\n"
    out_path = out_dir / (src.stem + ".md")
    out_path.write_text(header + body, encoding="utf-8")
    print(f"  ✓ {src.name} → {out_path.name}")


def main():
    OUTPUT_DIR.mkdir(exist_ok=True)
    files = sorted(SOURCE_DIR.iterdir())
    print(f"Processing {len(files)} files...")
    for f in files:
        write_markdown(f, OUTPUT_DIR)
    print("Done.")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 运行全部测试**

```bash
python -m pytest test_extract.py -v
```

预期：全部 `PASSED`

- [ ] **Step 5: 执行批量转换**

```bash
cd 国贸智能体 && python scripts/extract.py
```

预期：输出每个文件的转换结果，`knowledge_base/` 中生成对应 `.md` 文件。

- [ ] **Step 6: 确认输出文件**

```bash
ls knowledge_base/
```

预期：列出与 `国际经济学课上知识/` 中 PDF/PPTX 对应的 `.md` 文件。

- [ ] **Step 7: Commit**

```bash
git add scripts/extract.py scripts/test_extract.py knowledge_base/
git commit -m "feat: batch convert course materials to markdown knowledge base"
```
